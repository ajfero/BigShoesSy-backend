const { DetailCart } = require('../database/models/index');
const sequelize = require('sequelize');

// todo: CRUD of products
// Create a Cart and DetailCart
// const createProducts = async (req, res) => {

//   // get values on body request.
//   const { brand, colorway, imageUrl, styleId } = req.body

//   // create a new DetailCart
//   Product.create({

//     brand: brand,
//     colorway: colorway,
//     imageUrl: imageUrl,
//     styleId: styleId

//   }).then((detailCart) => {
//     return res.status(200).json({ status: 200, msg: "Created User Successeful!!", detailCart });
//   })
//     .catch((error) => {
//       // Create Profile error.
//       return res.status(400).json({ status: 400, msg: error });
//     });

// }

module.exports = {

  // createDetailCart

}
